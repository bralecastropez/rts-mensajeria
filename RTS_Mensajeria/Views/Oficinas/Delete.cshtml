@model RTS_Mensajeria.Models.DB_Model.Oficina
@{
    ViewBag.Title = "Eliminar oficina";
}
@if (!Request.IsAuthenticated)
{
    Response.Redirect(@Url.Action("Login", "Account"));
}
else
{

    <div class="card card-cascade narrower">
        <div class="view view-cascade primary-color narrower py-2 mx-4 mb-3 d-flex justify-content-between align-items-center">
            <h4 class="ml-3 mb-2 mb-sm-0 pt-1 white-text">
                <span><a href="@Url.Action("Index","Home")" class="text-decoration-none white-text"><i class="fa fa-home mr-1"></i> Inicio</a></span>
                <span>/</span>
                <span><a href="@Url.Action("Index", "Oficinas")" class="text-decoration-none white-text"><i class="fa fa-briefcase mr-1"></i> Oficinas</a></span>
                <span>/</span>
                <span>Eliminar oficina</span>
            </h4>
            <div class="mr-3">
                <button type="button" class="btn btn-outline-white btn-rounded btn-sm px-2">
                    <i class="fas fa-info-circle mt-0"></i>
                </button>
            </div>
        </div>
        <div class="px-4">
            <div class="table-wrapper">
                <br />
                <div class="row">
                    <div class="col-md-10">
                        <h2>Eliminar la oficina</h2>
                    </div>
                </div>
                <hr />
                <div>
                    <dl class="dl-horizontal">
                        <dt>
                            @Html.DisplayNameFor(model => model.Nombre)
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.Nombre)
                        </dd>
                        <dt>
                            @Html.DisplayNameFor(model => model.Ocupada)
                        </dt>

                        <dd>
                            @if (Model.Ocupada != 0)
                            {
                                <p>Ocupada</p>
                            }
                            else
                            {
                                <p>No ocupada</p>
                            }

                        </dd>

                        <dt>
                            @Html.DisplayNameFor(model => model.Nivel.Ubicacion)
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.Nivel.Ubicacion)
                        </dd>

                    </dl>


                    <hr />
                    <div class="text-center">
                        <h3>¿Desea eliminar ésta oficina?</h3>
                        @using (Html.BeginForm())
                        {
                            @Html.AntiForgeryToken()

                            <div class="form-actions no-color">
                                <a class="btn btn-primary" href="@Url.Action("Index", "Oficinas")">
                                    <i class="fa fa-arrow-left mr-1"></i> Regresar
                                </a>
                                |
                                <button type="submit" class="btn btn-default">
                                    <i class="fa fa-times mr-1"></i>
                                    Eliminar
                                </button>
                            </div>
                            <br />
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
}

